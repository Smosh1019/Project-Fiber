# Render.com deployment config (optional - auto-detected)
services:
  - type: web
    name: ulundi-fiber-map
    env: static
    buildCommand: echo "No build needed"
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
